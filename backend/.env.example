# Copy to backend/.env and set values
OPENAI_API_KEY=
# Optional: model name to use with the hosted API
MODEL_NAME=gpt-3.5-turbo
# Optional: set PROVIDER_URL if calling a non-OpenAI-compatible endpoint
# PROVIDER_URL=https://your-inference-endpoint.example/v1/chat/completions
